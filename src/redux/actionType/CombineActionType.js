import * as CounterActionTypes from './CounterActionType';
import * as ThemeActionTypes from './ThemeActionType';

const ActionTypes = ({
    CounterActionTypes,
    ThemeActionTypes
})

export default ActionTypes;